# Copy to `.env.local` — do not commit real secrets.

MONGODB_URI=

# Cloudinary (admin CMS image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# WhatsApp CTA on public Product / Services / Insurance enquiry modals (country code + number, digits only, e.g. 919876543210)
NEXT_PUBLIC_WHATSAPP_NUMBER=
